import React from 'react';

const Loader = () => (
  <div className="has-text-centered">
    <button className="button is-loading is-large is-white" style={{ border: 'none', background: 'none' }} />
  </div>
);

export default Loader;
